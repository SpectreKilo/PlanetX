<div class="header center-align">
    <h2>Warp Speed Activated</h2>
</div>
<div class="planet container center-align">
    <div>
        <h3>{{subgenre_name}}</h3>
    </div>
   
    {{#each warpDisp}}
    <div>
        <h3>{{topic}}</h3>
    <p>{{content}}</p>
    </div>
    {{/each}}
</div>