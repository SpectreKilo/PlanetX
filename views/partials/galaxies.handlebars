      {{!--displays our genre of topics  --}}
      <div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <h3><span class="card-title">{{genre_name}}</span></h3>
          <p>Is this working? this is the galaxies page</p>
          {{#each sub_genres}}
          <p>{{sub_genres.subgenre_name}}</p>
  {{/each}}
        </div>
      </div>
    </div>
  </div>