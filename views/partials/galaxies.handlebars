      {{!--displays our genre of topics  --}}
      <div class="row">
    <div class="col s12 m6">
      <div class="card purple darken-4">
        <div class="card-content white-text">
          <h3><span class="card-title">{{genre_name}}</span></h3>
          {{#each sub_genres}}
          <button class="{{subgenre_name}}" id="Btn" value='{{id}}'>{{subgenre_name}}</button>
  {{/each}}
        </div>
      </div>
    </div>
  </div>

