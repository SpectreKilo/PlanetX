      {{!--displays our genre of topics  --}}
      <div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title">{{genre.genre_name}}</span>
          {{#each SubGenre.genre_id}}
          <p>{{subgenre_name}}</p>
          {{/each}}
        </div>
      </div>
    </div>
  </div>