      {{!--displays our moons  --}}

      <div class="postForm">
        {{> moonForm}}
      </div>
      <div class="row center-align">
    <div class="col s12 m6 ">
      <div class="card purple darken-4">
        <div class="card-content white-text">
          <h3><span class="card-title center-align">{{subgenre_name}}</span></h3>
          {{#if topics}}
          {{#each topics}}
          <button type="submit" class="btn {{subgenre_name}}" id="post" value='{{id}}'>{{topic}}</button>
  {{/each}}
  {{/if}}
        </div>
      </div>
    </div>
  </div>
 <script src="/js/postBtn.js"></script>